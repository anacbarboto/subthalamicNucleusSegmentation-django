# subthalamicNucleusSegmentation-django
Subthalamic Nucleus Segmentation

pip3 install django
pip install djangorestframework
pip install django-cors-headers
pip install antspyx
pip install wheel
pip install antspyx
